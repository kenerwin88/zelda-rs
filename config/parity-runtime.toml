# Optional hot-reloaded timing diagnostics. The authoritative parity runner
# supplies an empty configuration; checked-in defaults must never alter game
# scheduling. Add rules only to a separate, explicitly selected experiment
# file while investigating a mismatch.
#
# Pre-slice NMI experiment: run the vblank boundary before an exact CPU slice.
# boot_init_only_host_frame = 81
# boot_init_only_main = 0
# boot_init_only_sub = 0
# boot_init_only_pending = 0
# boot_init_only_disable = 0
